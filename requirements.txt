# Bibliotecas Essenciais
requests==2.31.0             # Para fazer requisições HTTP
beautifulsoup4==4.12.2       # Parsear HTMLXML (web scraping)
pandas==2.1.1                # Manipulação de dados (CSVJSON)
python-dotenv==1.0.0         # Gerenciar variáveis de ambiente (.env)

# Parsers Alternativos (opcional, mas recomendado)
lxml==4.9.3                  # Parser rápido para Beautiful Soup

# Controle de Requisições (evitar bloqueios)
fake-useragent==1.3.0        # Gerar User-Agents realistas
requests-html==0.10.0        # Renderizar JavaScript (se necessário)

# Testes (opcional)
pytest==7.4.2                # Framework de testes